name: tokendashboard-plugin-claude
services:
  dev:
    build:
      context: .
    container_name: tokendashboard-plugin-claude-dev
    hostname: dev
    volumes:
      - ..:/workspaces/tokendashboard-plugin-claude
      - ~/.claude:/home/vscode/.claude
      - ~/.claude.json:/tmp/host-claude.json:ro
    environment:
      - NODE_ENV=development
    command: sleep infinity
    restart: unless-stopped
